<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.prototype2">

    <!--Add the permission for connecting to the internet-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/no_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/intro_image"
        android:screenOrientation="sensor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.IncidentListView" />
        <activity android:name=".views.AddReportActivity" />
        <activity android:name=".views.CoachAdviceActivity" />
        <activity android:name=".views.MemoryActivity" />
        <activity android:name=".views.SymptomsActivity" />
        <activity android:name=".views.CoachRegistrationActivity" />
        <activity android:name=".views.RedFlagActivity" />
        <activity android:name=".views.PlayerListViewActivity" />
        <activity android:name=".views.AllIncidentsListView" />
        <activity android:name=".views.PlayerProfile" />
        <activity android:name=".views.PlayerLoginActivity" />
        <activity android:name=".views.RecoveryActivity" />
        <activity android:name=".views.PlayerRegistrationActivity" />
        <activity android:name=".views.CoachProfile" />
        <activity android:name=".views.ObservableSignsActivity" />
        <activity android:name=".views.CoachLoginActivity" />
        <activity android:name=".views.AppInformationPage" />
        <activity android:name=".views.ReturnToSportStrategy" />
        <activity android:name=".views.AppCoachInformationPage" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>